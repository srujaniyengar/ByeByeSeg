CC = gcc
CFLAGS = -Wall -Wextra -pedantic -std=c11

SRC = safealloc.c example.c
OBJ = $(SRC:.c=.o)
TARGET = example

all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(CFLAGS) -o $@ $^

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJ) $(TARGET)

.PHONY: all clean
